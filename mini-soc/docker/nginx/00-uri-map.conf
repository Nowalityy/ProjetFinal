# Si un client ou un proxy upstream envoie REQUEST_URI au format absolu
# ("http://nginx/api/login"), ne garder que path + query pour PHP / Symfony.
map $request_uri $symfony_request_uri {
    default $request_uri;
    ~*^https?://[^/]+(/.*)$ $1;
}
